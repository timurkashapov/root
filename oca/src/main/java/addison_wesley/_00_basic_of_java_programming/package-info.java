/**
 * Topics (Темы)
 *
 * - Terminology and Concepts (Терминология и Концепции)
 *
 * - Abstractions (Абстракции)
 *
 * - Class (Класс / Категория)
 * - Object (Объект)
 *
 * - Contract (Контракт)
 * - Implementation (Реализация)
 *
 * - Constructor (Конструктор)
 *
 * - Properties (Свойство)
 *     - Attribute (Атрибут)
 *         - Field (Поле)
 *             - Variable (Переменная)
 *             - Value (Значение)
 *
 * - Behavior (Поведение)
 *     - Operations (Операция)
 *         - Method (Метод)
 *
 *
 * - Instantiation (Создание экземпляра)
 * - Class instantiation (Процесс создания экзепляра класса)
 * - Reference (ссылка)
 * - Reference value (значение ссылки)
 * - Declaration statement (Инструкция объявления)
 * - Assignment operator (Оператор присваивания [=] )
 * - Instance create operator (Оператор создания экземпляра класса [new])
 *
 * - Object Aliases (объектные псевдонимы)
 *
 * - Automatic garbage collector (GC Автоматический сборщик мусора)
 *
 * - Instance members (члены экземпляра)
 *     - Instance variables (переменные экземпляра)
 *         - State (состояние)
 *     - Instance methods (методы экземпляра)
 *
 * - Static members (статические члены)
 *
 */
package addison_wesley._00_basic_of_java_programming;
