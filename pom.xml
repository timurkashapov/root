<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.shako</groupId>
  <artifactId>root</artifactId>
  <version>1.0-SNAPSHOT</version>

  <name>root</name>
  <description>root</description>
  <url>http://www.shako.com</url>

  <properties>
      <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
      <maven.compiler.source>1.7</maven.compiler.source>
      <maven.compiler.target>1.7</maven.compiler.target>

      <junit.version.release>RELEASE</junit.version.release>
      <junit.version>3.8.1</junit.version>

      <gral.version.release>RELEASE</gral.version.release>
      <gral.version>0.11</gral.version>

      <maven.clean.plugin.version.release>RELEASE</maven.clean.plugin.version.release>
      <maven.clean.plugin.version> 3.0.0 </maven.clean.plugin.version>

      <maven.site.plugin.version.release>RELEASE</maven.site.plugin.version.release>
      <maven.site.plugin.version> 3.7 </maven.site.plugin.version>

      <maven.project.info-reports-plugin.version.release>RELEASE</maven.project.info-reports-plugin.version.release>
      <maven.project.info-reports-plugin.version> 2.9 </maven.project.info-reports-plugin.version>

      <maven.resources.plugin.version.release>RELEASE</maven.resources.plugin.version.release>
      <maven.resources.plugin.version> 3.0.2 </maven.resources.plugin.version>

      <maven.compiler.plugin.version.release>RELEASE</maven.compiler.plugin.version.release>
      <maven.compiler.plugin.version> 3.7.0 </maven.compiler.plugin.version>

      <maven.surefire.plugin.version.release>RELEASE</maven.surefire.plugin.version.release>
      <maven.surefire.plugin.version> 2.20.1 </maven.surefire.plugin.version>

      <maven.jar.plugin.version.release>RELEASE</maven.jar.plugin.version.release>
      <maven.jar.plugin.version> 3.0.2 </maven.jar.plugin.version>

      <maven.install.plugin.version.release>RELEASE</maven.install.plugin.version.release>
      <maven.install.plugin.version> 2.5.2 </maven.install.plugin.version>

      <maven.deploy.plugin.version.release>RELEASE</maven.deploy.plugin.version.release>
      <maven.deploy.plugin.version> 2.8.2 </maven.deploy.plugin.version>

  </properties>

  <dependencies>

      <dependency>
          <groupId>junit</groupId>
          <artifactId>junit</artifactId>
          <version>${junit.version.release}</version>
      </dependency>

      <!-- https://mvnrepository.com/artifact/de.erichseifert.gral/gral-core -->
      <dependency>
          <groupId>de.erichseifert.gral</groupId>
          <artifactId>gral-core</artifactId>
          <version>${gral.version.release}</version>
      </dependency>

  </dependencies>

  <build>
    <pluginManagement><!-- lock down plugins versions to avoid using Maven defaults (may be moved to parent pom) -->
      <plugins>
        <plugin>
          <artifactId>maven-clean-plugin</artifactId>
          <version>${maven.clean.plugin.version.release}}</version>
        </plugin>
        <plugin>
          <artifactId>maven-site-plugin</artifactId>
          <version>${maven.site.plugin.version.release}</version>
        </plugin>
        <plugin>
          <artifactId>maven-project-info-reports-plugin</artifactId>
          <version>${maven.project.info-reports-plugin.version.release}</version>
        </plugin>
        <!-- see http://maven.apache.org/ref/current/maven-core/default-bindings.html#Plugin_bindings_for_jar_packaging -->
        <plugin>
          <artifactId>maven-resources-plugin</artifactId>
          <version>${maven.resources.plugin.version.release}</version>
        </plugin>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven.compiler.plugin.version.release}</version>
        </plugin>
        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven.surefire.plugin.version.release}}</version>
        </plugin>
        <plugin>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${maven.jar.plugin.version.release}}</version>
        </plugin>
        <plugin>
          <artifactId>maven-install-plugin</artifactId>
          <version>${maven.install.plugin.version.release}</version>
        </plugin>
        <plugin>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>${maven.deploy.plugin.version.release}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <reporting>
    <plugins>
      <plugin>
        <artifactId>maven-project-info-reports-plugin</artifactId>
      </plugin>
    </plugins>
  </reporting>

</project>
